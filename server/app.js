const express = require("express");
const app = express();

// MIDDLEWARES
app.use(express.json());

// ROUTES
// DEFINE ROUTES HERE OR IMPORT FROM A SEPERATE ROUTES FILE

module.exports = app;
